import { Form, Input, Button, Checkbox } from 'antd';

const home = () => {

    return (
        <div>
            
            <h2>home</h2>
                   
        </div>
    );
}

export default home;