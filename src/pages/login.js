import { Form, Input, Button, Checkbox } from 'antd';

const login = () => {

    return (
        <div>
            
            <h2>login</h2>
                   
        </div>
    );
}

export default login;