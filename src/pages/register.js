import { Form, Input, Button, Checkbox } from 'antd';

const register = () => {

    return (
        <div>
            
            <h2>register</h2>
                   
        </div>
    );
}

export default register;