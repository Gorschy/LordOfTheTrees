import { Form, Input, Button, Checkbox } from 'antd';

const calculator = () => {

    return (
        <div>
            
            <h2>calculator</h2>
                   
        </div>
    );
}

export default calculator;